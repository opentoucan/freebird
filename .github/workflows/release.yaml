name: release

on:
  workflow_dispatch:
  push:
    branches: ["main", "release-*"]
    paths:
      - '!target/**'
      - '!CHANGELOG.md'
      - '!README.md'

env:
  REGISTRY: ghcr.io

jobs:
  release-please:
    runs-on: ubuntu-latest
    if: github.repository == 'opentoucan/freebird'
    outputs:
      release_created: ${{ steps.release.outputs.release_created }}
      tag_name: ${{ steps.release.outputs.tag_name }}
    permissions:
      contents: write
      packages: write
      id-token: write
      pull-requests: write
    steps:
    - uses: googleapis/release-please-action@v4
      id: release
      with:
        token: ${{ secrets.GITHUB_TOKEN }}
        config-file: release-please-config.json
        manifest-file: .release-please-manifest.json

  docker-build:
    runs-on: ubuntu-latest
    needs: release-please
    if: ${{ needs.release_please.outputs.release_created == 'true' }}
    permissions:
      contents: write
      packages: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.PAT }}

      - name: Log into registry ${{ env.REGISTRY }}
        if: github.event_name != 'pull_request'
        uses: docker/login-action@28218f9b04b4f3f62068d7b6ce6ca5b26e35336c
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract Docker metadata
        id: meta
        uses: docker/metadata-action@98669ae865ea3cffbcbaa878cf57c20bbf1c6c38
        with:
          images: ${{ env.REGISTRY }}/${{ github.repository_owner }}/freebird
          tags: |
            type=semver,pattern=${{ needs.release_please.outputs.tag_name }}
            type=raw,value=latest

      - name: Build and push Docker image
        id: build-and-push
        uses: docker/build-push-action@ac9327eae2b366085ac7f6a2d02df8aa8ead720a
        with:
          context: .
          file: ./Dockerfile
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}

      - name: Release
        uses: softprops/action-gh-release@v2
        if: startsWith(github.ref, 'refs/tags/')
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          tag_name: ${{ needs.release_please.outputs.tag_name }}
          files: |
            CHANGELOG.md

      - name: Branch from tag
        run: |
          git checkout -b release-${{ needs.release_please.outputs.tag_name }} ${{ needs.release_please.outputs.tag_name }}
          git push -u origin ${{ needs.release_please.outputs.tag_name }}
